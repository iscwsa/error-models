# Error Model Sub-Committee

### <ins>**Error Model Schema**</ins>

<ins>**Goals**</ins>

- Create Error Model JSON schema in support of a common exchange format within OSDU and the industry at large
- Migrate existing Error Model definitions from Excel to JSON **(\*)**

**(\*)** Excluding legacy models
